{
  "name": "personal-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-domain personal agent + glanceable surface. See docs/.",
  "scripts": {
    "check": "node --check prototype/scriptable/glance-widget.js && node --check prototype/scriptable/glance-app.js"
  }
}
